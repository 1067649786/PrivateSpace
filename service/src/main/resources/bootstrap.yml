spring:
  application:
    name: service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: properties
        shared-dataids: service.properties
        refreshable-dataids: service.properties
